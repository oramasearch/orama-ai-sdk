{
  "name": "ai-sdk-orama-provider",
  "version": "0.1.0",
  "description": "Orama provider for Vercel AI SDK",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --dts --format cjs,esm",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@oramacloud/client": "^1.0.0"
  },
  "peerDependencies": {
    "ai": "^4.0.0"
  },
  "devDependencies": {
    "ai": "^4.0.0",
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}